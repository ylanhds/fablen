spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.0.135:3306/product_db?useSSL=false&serverTimezone=UTC
    username: product_user
    password: Adm!nP@ss2024
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:192.168.0.135:8848}
        namespace: ${NACOS_NAMESPACE:df35fbb2-86a5-492c-bd80-cf2e8a51e53b}
        group: DEFAULT_GROUP
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:YourSecurePassword123}
      config:
        server-addr: ${NACOS_SERVER_ADDR:192.168.0.135:8848}
        namespace: ${NACOS_NAMESPACE:df35fbb2-86a5-492c-bd80-cf2e8a51e53b}
        group: DEFAULT_GROUP
        file-extension: yaml
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:YourSecurePassword123}
        refresh-enabled: true
        # 添加共享配置
        shared-configs:
          - data-id: dubbo-common.yaml
            group: DEFAULT_GROUP
            refresh: true
  server:
    port: 9002
  application:
    name: cloud-product
  jwt:
    secretKey: dJy5MH1u73qIRu74OuqKHTALBUGqVfyAoFLiCUnwl4Y=
    expiration: 86400000 # 24小时


dubbo:
  protocol:
    port: 20881  # 不同端口避免冲突


# MyBatis Plus 配置
mybatis-plus:
  configuration:
    # 开启下划线到驼峰命名的自动转换
    map-underscore-to-camel-case: true
    # 打印 SQL 日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键策略
      id-type: auto
      # 逻辑删除字段
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  # Mapper XML 文件位置（如果使用 XML）
  mapper-locations: classpath*:/mapper/**/*.xml

