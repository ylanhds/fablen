eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://interview-eureka:8761/eureka
spring:
  application:
    name: interview-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://127.0.0.1"
            allowedMethods: GET, POST, PUT, DELETE, OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
      ## 启用服务发现路由支持
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: interview-auth
          uri: lb://interview-auth
          predicates:
            - Path=/auth/api/**
          filters:
            - RewritePath=/auth/(?<segment>.*), /$\{segment}

        - id: interview-product
          uri: lb://interview-product
          predicates:
            - Path=/product/api/products/**
          filters:
            - RewritePath=/product/(?<segment>.*), /$\{segment}
logging:
  level:
    root: INFO
    com.interview.gateway: DEBUG
    org.springframework.cloud.gateway: TRACE
server:
  port: 7573